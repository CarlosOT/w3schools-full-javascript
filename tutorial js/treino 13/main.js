import * as mds from "./modules/script.js"


let n1 = 23.43;
// mds.htmlWrite('demo', Math.sqrt(n1));
// mds.htmlWrite('demo', Math.random(n1));
// mds.htmlWrite('demo', Math.round(n1));
// mds.htmlWrite('demo', Math.ceil(n1));
// mds.htmlWrite('demo', Math.min(2,4,5,1,3,4,3,1,2,3,1,31,41,2));
mds.htmlWrite('demo', Math.max(2,4,5,1,3,4,3,1,2,3,1,31,41,2));

